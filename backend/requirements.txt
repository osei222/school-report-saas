# Django Core
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1

# Authentication
djangorestframework-simplejwt==5.3.0
PyJWT==2.8.0

# Database (for production)
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Production server
gunicorn==21.2.0

# Static files
whitenoise==6.6.0

# Image processing and PDF generation (Python 3.11 compatible versions)
Pillow>=9.0.0,<11.0.0
qrcode[pil]==7.4.2
reportlab==4.0.4

# Utilities
python-decouple==3.8
python-dotenv==1.0.0
requests==2.31.0

# NOTE: Optional packages to add later (may lack wheels on very new Python versions):
# psycopg2-binary==2.9.9         # PostgreSQL driver (only if using Postgres locally)
reportlab==4.0.7               # PDF generation
# WeasyPrint==60.1               # Alternative PDF renderer
# cloudinary==1.36.0             # Cloud image storage
# pandas==2.1.3                  # Heavy data processing (bulk ops)
# python-dateutil==2.8.2         # Date utilities
